# Compatibility layer
CFLAGS += -isystem ../../os/include/compat
CFLAGS += --include compat/polycube.h

# The code does pointer arithmetic with void*
CFLAGS += -Wno-pointer-arith

# The code declares unused labels
CFLAGS += -Wno-unused-label
